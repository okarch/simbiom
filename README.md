simbiom - REST API to serve biosample information from the biobank
==================================================================

Backend service to support CRUD operations on the biosample inventory through a REST interface.
